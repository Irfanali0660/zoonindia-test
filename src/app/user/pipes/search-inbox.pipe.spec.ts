import { SearchInboxPipe } from './search-inbox.pipe';

describe('SearchInboxPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchInboxPipe();
    expect(pipe).toBeTruthy();
  });
});
